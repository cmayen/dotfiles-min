--- .patches/dot_bashrc.original	2025-11-11 20:43:42.172411861 +0000
+++ /home/vscode/.bashrc	2025-11-11 20:44:29.828504215 +0000
@@ -113,9 +113,12 @@
 fi
 # bash theme - partly inspired by https://github.com/ohmyzsh/ohmyzsh/blob/master/themes/robbyrussell.zsh-theme
 __bash_prompt() {
+#    local userpart='`export XIT=$? \
+#        && [ ! -z "${GITHUB_USER:-}" ] && echo -n "\[\033[0;32m\]@${GITHUB_USER:-} " || echo -n "\[\033[0;32m\]\u " \
+#        && [ "$XIT" -ne "0" ] && echo -n "\[\033[1;31m\]➜" || echo -n "\[\033[0m\]➜"`'
     local userpart='`export XIT=$? \
-        && [ ! -z "${GITHUB_USER:-}" ] && echo -n "\[\033[0;32m\]@${GITHUB_USER:-} " || echo -n "\[\033[0;32m\]\u " \
         && [ "$XIT" -ne "0" ] && echo -n "\[\033[1;31m\]➜" || echo -n "\[\033[0m\]➜"`'
+
     local gitbranch='`\
         if [ "$(git config --get devcontainers-theme.hide-status 2>/dev/null)" != 1 ] && [ "$(git config --get codespaces-theme.hide-status 2>/dev/null)" != 1 ]; then \
             export BRANCH="$(git --no-optional-locks symbolic-ref --short HEAD 2>/dev/null || git --no-optional-locks rev-parse --short HEAD 2>/dev/null)"; \
@@ -130,7 +133,8 @@
         fi`'
     local lightblue='\[\033[1;34m\]'
     local removecolor='\[\033[0m\]'
-    PS1="${userpart} ${lightblue}\w ${gitbranch}${removecolor}\$ "
+    #PS1="${userpart} ${lightblue}\w ${gitbranch}${removecolor}\$ "
+    PS1="\n${userpart} ${lightblue}\w ${gitbranch}${removecolor}\n\$ "
     unset -f __bash_prompt
 }
 __bash_prompt
@@ -155,3 +159,34 @@
     # Append to PROMPT_COMMAND to call precmd before displaying the prompt
     PROMPT_COMMAND="${PROMPT_COMMAND:+$PROMPT_COMMAND; }precmd"
 fi
+
+
+export LANG="en_US.UTF-8"
+#source <(kubectl completion bash)
+
+
+# start agent if not already running
+if [ -z "$SSH_AUTH_SOCK" ]; then
+    eval "$(ssh-agent -s)" >/dev/null
+    ssh-add ~/.ssh/id_ed25519 2>/dev/null
+fi
+
+
+
+
+# Check if Git user.name is already set globally
+if ! git config --global user.name > /dev/null 2>&1; then
+    read -p "Enter your GitHub username: " github_username
+    git config --global user.name "$github_username"
+fi
+# Check if Git user.email is already set globally
+if ! git config --global user.email > /dev/null 2>&1; then
+    read -p "Enter your GitHub email: " github_email
+    git config --global user.email "$github_email"
+fi
+
+
+
+
+
+
